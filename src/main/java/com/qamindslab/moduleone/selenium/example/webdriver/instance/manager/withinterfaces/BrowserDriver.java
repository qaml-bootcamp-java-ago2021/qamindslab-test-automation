package com.qamindslab.moduleone.selenium.example.webdriver.instance.manager.withinterfaces;

import org.openqa.selenium.WebDriver;

public interface BrowserDriver {
    WebDriver getInstanceBrowserDriver();
}
